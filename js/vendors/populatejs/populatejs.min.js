!(function (e) {
  var t = function (e) {
    for (var t in e) {
      var a = e[t],
        n = document.getElementsByName(t);
      if (n.length)
        switch (n[0].nodeName.toLowerCase()) {
          case "input":
            switch (n[0].getAttribute("type")) {
              case "checkbox":
                1 == a || 0 == a || "checked" == a
                  ? ((n.checked = a), (n[0].checked = a))
                  : (a = a.split(",")).map(function (e) {
                      for (var t = 0; t < n.length; t++)
                        n[t].value == e && (n[t].checked = !0);
                    });
                break;
              case "radio":
                for (var c = 0; c < n.length; c++)
                  n[c].value == a && (n[c].checked = !0);
                break;
              default:
                n[0].value = a;
            }
            break;
          case "select":
            if (n[0].hasAttribute("multiple")) {
              a = a.split(",");
              var o = n[0].getElementsByTagName("option");
              for (c = 0; c < o.length; c++)
                -1 != a.indexOf(o[c].value) && (o[c].selected = !0);
            } else n[0].value = a;
          case "textarea":
            n[0].value = a;
            break;
        }
    }
  };
  "function" == typeof define && "object" == typeof define.amd && define.amd
    ? define(function () {
        return t;
      })
    : "undefined" != typeof module && module.exports
    ? (module.exports = t)
    : (e.populatejs = t);
})(this);
